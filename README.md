yarn add -D typescript @types/node @types/express ts-node nodemon

# TypeScript & Node
yarn add -D typescript @types/node @types/express ts-node nodemon

# Drizzle CLI for migrations
yarn add -D drizzle-kit

# GraphQL Codegen - generates TypeScript types
yarn add -D @graphql-codegen/cli
yarn add -D @graphql-codegen/typescript
yarn add -D @graphql-codegen/typescript-resolvers

yarn add express graphql
yarn add @apollo/server          # GraphQL server
yarn add drizzle-orm postgres    # Database
yarn add dotenv                  # Environment variables